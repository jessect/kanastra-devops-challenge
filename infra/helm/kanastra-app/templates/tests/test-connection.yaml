apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kanastra-app.fullname" . }}-test-connection"
  labels:
    {{- include "kanastra-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kanastra-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
